#first
"Hello World"

#simplecalculation
10+20

#print
print ("Hello World")
print (10+20)

#variable
a = 5 
print (a)

#vector
c(3,10,15)
c(1:5)

#summary
summary(c(1:5))

#variable
budi_berat_kg <- 68
santi_berat_kg <- 54.5
budi_berat_kg
santi_berat_kg
pi<- 3.14
pi

#vector
c(1,2,3)
ayam <- c(1,2,3)
print(ayam)

nama_mahasiswa <- c("Amira", "Budi", "Charlie")
print (nama_mahasiswa)

c(1:10)
bebek <- c(1:10)
print(bebek[5]))
print(bebek[[1:5]])

kode_prodi <- c("DKV","ILKOM","ICT") # Buat vector teks dengan nama kode_prodi yang diisi sesuai petunjuk soal

print (kode_prodi[3])# Tampilkan isi indeks ketiga dari kode_prodi

#Membuat named vector dengan nama nilai
nilai <- c(statistik = 89, fisika = 95, ilmukomunikasi = 100)

#Menampilkan isi variable nilai
print(nilai)

print (nilai["fisika"])#Menampilkan isi dengan nama fisika

profil <- c(nama = "Budi", tempat_tinggal = "Jakarta", tingkat_pendidikan = "S1")#Buat variable profil sesuai permintaan soal

print(profil) #Tampilkan variable profil

#variable
# List disimpan dalam variable dengan nama list_random
list_random <- list(2, "Budi", 4)

# Menampilkan isi list
list_random 

# List disimpan dalam variable dengan nama dati2
dati2 <- list(nama = "Denpasar", propinsi = "Bali")

# Menampilkan isi list dati2
dati2 

kota <- list(nama_kota = "Makassar", propinsi = "Sulawesi Selatan", luas_km2 = 199.3) # Buat variable kota sesuai permintaan soal

# Tampilkan isi variable list kota
kota

list_satu <- list(1, "Online", TRUE )
list_satu[1]
